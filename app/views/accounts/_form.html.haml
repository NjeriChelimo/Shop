= simple_form_for(:account, :url => {:action => 'create'}) do |f|
  = f.error_notification

  .form-inputs
    = f.label :name, "Name"
    = f.text_field :name

  .form-actions
    = f.button :submit
