%h1 Listing accounts

%table
  %tr
    %th
    %th
    %th

  - @accounts.each do |account|
    %tr
      %td= account.name
      %td= account.id
      %td= link_to 'Edit', edit_organization_account_path(@organization.id, account.id), :class=>"btn"
      %td= link_to 'Account details', organization_account_path(@organization.id, account.id), :class=>"btn"
      //%td= link_to 'Destroy', account, method: :delete, data: { confirm: 'Are you sure?' }
      %td= link_to "Images", organization_account_path(@organization.id, account.id), :class=>"btn"
